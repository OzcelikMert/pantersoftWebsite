<section id="intro">
    <div class="masthead">
        <div class="container px-4 px-lg-5 d-flex h-100 align-items-center justify-content-center">
            <div class="masthead-content d-flex justify-content-center">
                <% if(typeof slider !== "undefined") { %> 
                    <%-slider%>
                <% } %> 
            </div>
        </div>
    </div>
</section>