class View {
    TABLE_NAME = "view";
    ID = `${this.TABLE_NAME}.id`;
    IP = `${this.TABLE_NAME}.ip`;
    URL = `${this.TABLE_NAME}.url`;
    DATE = `${this.TABLE_NAME}.date`;
    ALL = `${this.TABLE_NAME}.*`;
}

export default View;